---
- hosts: vm1-db-u
  become: yes
  tasks:
  - name: ensure prequest is installed
    apt:
      name: "{{item}}"
      update_cache: yes
    loop:
      - openjdk-11-jdk
      - ansible

  - name: copy the nginx key file
    copy:
      src:
      dest:
      mode: '0600'
    notify:
