---
- hosts : vm1-db-u
  become: yes
  tasks:
  - name: ensure prequest is installed
    apt:
      name          : "{{item}}"
      update_cache  : yes
      state         : latest
    loop:
      - openjdk-11-jdk
      - ansible
